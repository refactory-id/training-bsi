class City {
  final String id, name, provinceId, province, type, postalCode;

  City(
      {this.id,
      this.name,
      this.provinceId,
      this.province,
      this.type,
      this.postalCode});
}
